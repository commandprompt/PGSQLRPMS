diff -Nur postgresql-8.2.16.orig/src/pl/plperl/GNUmakefile postgresql-8.2.16/src/pl/plperl/GNUmakefile
--- postgresql-8.2.16.orig/src/pl/plperl/GNUmakefile	2006-07-21 02:24:04.000000000 +0200
+++ postgresql-8.2.16/src/pl/plperl/GNUmakefile	2010-05-17 06:07:22.272643536 +0200
@@ -36,6 +36,9 @@
 
 SHLIB_LINK = $(perl_embed_ldflags) $(BE_DLLLIBS)
 
+# Force rpath to be used even though we disable it everywhere else
+SHLIB_LINK += $(rpath)
+
 REGRESS_OPTS = --dbname=$(PL_TESTDB) --load-language=plperl
 REGRESS = plperl plperl_trigger plperl_shared plperl_elog
 # where to find psql for running the tests
